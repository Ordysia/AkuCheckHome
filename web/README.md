# AkuCheckHome — aplikacja webowa

Kod aplikacji znajduje się w tym katalogu. Pełny opis projektu, instrukcja
uruchomienia, konfiguracja Supabase i zasady bezpieczeństwa są dostępne w
głównym pliku [`README.md`](../README.md).

Szybki start:

```bash
npm install
npm run dev
```
